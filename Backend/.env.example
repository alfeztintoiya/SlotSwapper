# Backend environment variables
PORT=4000
# For local dev, you can use local MongoDB:
# MONGO_URI=mongodb://localhost:27017/slotswapper
# For Atlas, use the SRV connection string (replace <user>, <pass>, <cluster>, and optional db name):
MONGO_URI=mongodb+srv://alfeztintoiya25:alfez25@cluster0.d4ya9.mongodb.net/slotswapper?retryWrites=true&w=majority&appName=cluster0.d4ya9
JWT_SECRET=supersecretjwtkey
CORS_ORIGIN=http://localhost:5173
# For multiple origins (e.g., Vercel preview + production), you can provide a comma-separated list:
# CORS_ORIGIN=https://your-app.vercel.app,https://your-preview-url.vercel.app
# In production with cross-site cookies, the backend will set cookies with SameSite=None;Secure automatically.
COOKIE_NAME=token
NODE_ENV=development
